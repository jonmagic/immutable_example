defaults: &defaults
  adapter: mysql2
  encoding: utf8
  collation: utf8_unicode_ci
  host: 127.0.0.1
  password:
  port: 3306
  username: root

development:
  <<: *defaults
  database: immutable_example_dev

test:
  <<: *defaults
  database: immutable_example_test
